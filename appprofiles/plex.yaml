meta:
  name: Plex
  category: Media
  icon: plex
  description: Personal media server and streaming platform
  homepage: https://www.plex.tv

# Meta-only profile. Plex webhooks require Plex Pass and are sent as
# multipart/form-data, which NORA's ingest handler does not currently parse.
# Until multipart ingest is supported, attach this profile for the name,
# icon, and category; use a URL monitor check for up/down signal.
