meta:
  name: Prowlarr
  category: Media
  icon: prowlarr
  description: Indexer manager and proxy for Usenet and torrents
  homepage: https://prowlarr.com

# Meta-only profile. Prowlarr isn't in active use here yet, so the webhook
# payloads haven't been validated end-to-end — shipping a half-baked mapping
# would mislabel events. When someone puts Prowlarr into production,
# reintroduce field_mappings / digest entries using real captured payloads.
