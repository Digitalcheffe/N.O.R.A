meta:
  name: Vaultwarden
  category: Security
  icon: vaultwarden
  description: Lightweight self-hosted Bitwarden-compatible password manager
  homepage: https://github.com/dani-garcia/vaultwarden

# Meta-only profile. Vaultwarden has no outbound webhooks and no public
# events worth ingesting. Attach this profile for the name, icon, and
# category; add URL + SSL monitor checks — this is a password manager and
# both availability and cert expiry matter.
