services:
  nora:
    build: .
    ports:
      - "8081:8081"
    volumes:
      - nora-data:/data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - NORA_SECRET=dev-secret-change-me
      - NORA_DB_PATH=/data/nora.db
      - NORA_PORT=8081
volumes:
  nora-data:
