package jobs

// RunMetricsCollection and its hourly scheduling have moved to
// internal/scanner/discovery/event_rollup.go where they run as a
// GlobalDiscoveryJob on the scan scheduler's 1-hour discovery tick.
